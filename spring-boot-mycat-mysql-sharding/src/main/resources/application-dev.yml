spring:
  # 配置数据源
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:8066/TESTDB
    # 这里配置的是 Mycat 中 server.xml 中配置账户密码，不是数据库的账户密码
    username: root
    password: 123456
mybatis:
  mapper-locations: mybatis/*Mapper.xml
  configuration:
    map-underscore-to-camel-case: true


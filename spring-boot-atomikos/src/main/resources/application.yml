server:
  port: 8000
spring:
  profiles:
    active: dev
  jta:
    atomikos:
      properties:
        max-actives: 50
        max-timeout: 300000
        default-jta-timeout: 10000
        enable-logging: true

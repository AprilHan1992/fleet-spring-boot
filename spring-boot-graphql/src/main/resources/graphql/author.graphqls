# 作者信息查询
extend type Query {
    # 通过 id 查询作者信息
    getAuthorById(id: Int!): Author
    # 查询作者信息列表
    list: [Author]
}

# 作者信息修改
extend type Mutation {
	# 新增作者信息
	insertAuthor(authorInput: AuthorInput!): Author
}
